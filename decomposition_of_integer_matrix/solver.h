#ifndef SOLVER_H
#define SOLVER_H
// eso de arrba siempre va para un .h, lo mismo aplica para la ultima linea, aqui solamente ponemos definiciones

#include <vector>


void decompose( std::vector< std::vector<int> >& );

void nofun();

#endif